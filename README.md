# PawPedia Web

React frontend for PawPedia, a cat breed explorer backed by `pawpedia-api`.

## Local Development

```sh
npm install
cp .env.example .env
npm run dev
```

The web app runs on `http://localhost:5173`.

## Scripts

```sh
npm test
npm run typecheck
npm run build
```
